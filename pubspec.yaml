name: super_field
description: A lightweight, extensible Flutter text field ecosystem for embedding interactive tokens (mentions, tags, chips) directly into text flow.
version: 0.1.0
homepage: https://github.com/nombrekeff/super_field

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
